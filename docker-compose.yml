services:
  db:
    image: postgres:17-alpine
    container_name: proyecto4-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: proyecto4
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d proyecto4"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
